{
  "name": "uxcode-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Static landing page for UXCode",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "python3 -m http.server 8080",
    "preview": "python3 -m http.server 8080 --directory dist"
  }
}
